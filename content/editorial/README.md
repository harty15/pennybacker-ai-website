# Editorial

What ships lives here; what is planned lives in the firm vault.

- `STYLE.md` — the writing law for Insights posts. Binding for humans and agents. Stays in this repo because
  the build and the drafting agents read it here.
- Queue, research, briefs, the article tracker, the image style guide, the routine prompts and the agent
  definitions live in the Pennybacker-Home vault under `2-Areas/content/` (Google Drive shared drive, synced
  to `~/Library/CloudStorage/GoogleDrive-ryan@pennybacker-ai.com/Shared drives/Pennybacker-Home`). The
  routines run there and write into this repo through pull requests.
- `queue.yaml` is a mirror slot for the cloud-routine fallback described in the vault's
  `routines/runbook.md`; it is empty while routines run locally.
